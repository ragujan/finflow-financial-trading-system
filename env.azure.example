# Copy to a local file (not committed) and load before quarkus:dev, e.g.:
#   Get-Content .env.azure.local | ForEach-Object {
#     if ($_ -match '^\s*([^#][^=]+)=(.*)$') { Set-Item -Path "Env:$($matches[1].Trim())" -Value $matches[2].Trim() }
#   }

EVENTHUBS_BOOTSTRAP=finflow-dev-eh.servicebus.windows.net:9093
EVENTHUBS_CONNECTION_STRING=Endpoint=sb://finflow-dev-eh.servicebus.windows.net/;SharedAccessKeyName=...;SharedAccessKey=...
